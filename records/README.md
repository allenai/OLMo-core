# Records

Standalone writeups that used to hang loose at the repo root: experiment diagnoses, task briefs
for agents, and setup notes. These are *reference documents* — still-valid knowledge, unlike
`deprecated/` (things that must not be used). New writeups of this kind go here, not the repo
root; the root keeps only README/CHANGELOG/CONTRIBUTING/CLAUDE.md/local_cluster.md/beaker.md.

| Doc | What it is |
|---|---|
| `ctc-suite-olmo-eval-handoff.md` | Handoff record + ready-to-post PR body for the 22-task suite on olmo-eval branch `prasann/ctc-suite`: what's on the branch, validation evidence, public-dataset access, and the open questions for maintainers. |
| `contradiction-data-and-base-hygiene.md` | **Load-bearing**: the running list of *silently wrong* contradiction shards, base checkpoints, sidecars and defaults — things that load, train, and yield a plausible-but-garbage number. Read before picking a base or a shard; check here before adding a new data build. Includes §4b, the FEVER/wiki filler leak into PubMed contradiction evals — **still OPEN for the CTC suite ladder** — and which eval bundle to use. |
| `contradiction-train-eval-non-iid.md` | **Load-bearing**: every contradiction number to date scores a `realistic`-mode-trained model on a `both`-mode eval — two different gold-pair generators. Affects the CTC suite *and* results-hub v2 (one shared training file). Fixing only the eval moves dense 0.559 → 0.946 f1 at n=762 and reproduces the paper. Also documents the outlier ladder's M mismatch (train max 10 categories vs eval 25, **still open**) and recovers the training run's unlogged `--max-overlap 0.5` from the data. |
| `document-chunked-marker-embeddings.md` | Diagnosis of Qwen3's untrained (bit-identical) marker-token embeddings + the `fix_marker_embeddings.py` repair. **Load-bearing**: read before any docchunk/landmark training from a fresh base (CLAUDE.md points here). |
| `multihop-gold-routing-experiment.md` | Experiment proposal: can a model use two gold docs that never directly attend to each other? (channel (c) = multi-hop routing across layers). Hop ladder + the leak-matched `hop∞` control. |
| `instruction-tuning-setup.md` | Instruction-tuning / longctx SFT pipeline setup notes (weka-era; some pointers superseded by `local_cluster.md`). |
| `landmark-packing-cp-task.md` | GPU-agent task brief: landmark attention + sequence packing + context parallelism (done). |
| `landmark-sparse-decode-task.md` | GPU-agent task brief: make landmark top-k decode O(k·block) (open). |
| `pooled-doc-kv-handoff.md` | **Pooled-doc-KV agent handoff** — operational pick-up-here doc: 32k parity result (v25 0.927 vs 0.939 @ 2.8×, zero full attention), the exact v25 recipe + launch/eval commands, closed axes (oracle slots, role-blind FFN gate, distill), open threads (64k/128k length-scaling campaign, learned null-expert router), asset/code maps, and the ops trap list. Read this first, the main record second. |
| `pooled-doc-kv-attention.md` | **Pooled-doc-KV attention** (`AttentionType.pooled_doc_kv`): train-time KV compression — most context docs collapse, for outside queries, to one mean-pooled KV slot with a `+log(doc_len)` logit bias (exactly ≡ full causal attention over a perturbed corpus); gold + a few random negatives keep real KV via the gold sidecar; inference is ordinary full attention. The math, honest speedup/memory expectations, transfer risks, implementation map, and the first `train_ctc_suite.py --variant pooledkv` experiment sketch. |
| `weka-checkpoint-cleanup.md` | The 2026-07-28 weka audit + cleanup (21.37 TB → 7.2 TB). Why `stepN/` dirs are redundant against the model-only `model_and_optim/` that eval reads, what was deleted, and the paths that must never be. Tooling in `debug/weka_cleanup/`. |
| `xabsence-learnability-probes.md` | Why xabsence never trained: four 0.8B probes (data scale 400×, smaller `P`, Gutenberg corpus, 50-example memorization control) all land **exactly on chance** held out, while the control fits its 50 training examples perfectly. **Load-bearing caveat**: the "CE 0.78 → 0.23–0.33" improvement was the *chance floor*, not progress — always compare CE to `ln(C(P+k,k))/T` and confirm with a generation metric. 2B is the open test. |
| `v3-eval-howto.md` | **How to run the v2 and v3 eval ladders** (for collaborators). v3 = v2_clean with contra rebuilt `realistic`-mode and outlier's xlong rungs rebuilt scale-K; nq/rerank/oolong are symlinks to v2 and must NOT be re-run. Exact launcher commands, the `--query-position` / `--tokenizer` traps that silently zero a run, and the v2/v3 result-filename split. |
| `ctc-figure-settings-unification.md` | Audit of every CTC figure in `paperdraft2` — which model scales / tasks / rungs / arms each one selects — plus the cross-axis coverage matrix (contradiction is the **only** task measured on all five of SUITE/SCALE/LENGTHGEN/FAMILY/MASKMIX) and three costed options for unifying them. **Load-bearing caveat**: the new outlier fix-k pure-chunked control (mean mask-mixing gain **+0.818**, class $O_T(N)$, flat CE) breaks TABLE-MASKMIX's "mixing is worth ~0 on $O_T(N)$" headline — the gain tracks whether the pure-chunked run *fit*, not the CTC class. |
