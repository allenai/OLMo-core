"""Corpus-reasoning: long-context task data generation, evaluation harness, and viz.

Ported from the standalone PrasannS/corpus-reasoning repo so this tree is self-contained.
Import as ``corpus_reasoning.*`` (``PYTHONPATH=$PWD/src``). The large tokenized corpora and
checkpoints stay on /scratch -- only code lives here.
"""
