# build artifacts

.eggs/
.mypy_cache
*.egg-info/
build/
dist/
pip-wheel-metadata/
private/
tmp/

# dev tools

.envrc
.python-version
.idea
.venv/
.vscode/
.cursor/
/*.iml
pyrightconfig.json
.ruff.toml
uv.lock


# jupyter notebooks

.ipynb_checkpoints


# miscellaneous

.cache/
doc/_build/
*.swp
.DS_Store


# python

*.pyc
*.pyo
__pycache__


# testing and continuous integration

.coverage
.pytest_cache/
.benchmarks

# documentation build artifacts

docs/build
site/

# runs
/runs/
/wandb/
/scratch/
core
/dataset-cache/

# slurm job logs: old sbatch launchers wrote hidden `.<family>_%x_%j.log` files to the repo root;
# those now live in /logs. Both patterns ignored so stale launchers don't re-clutter git status.
/logs/
/.*.log

# runtime artifacts: gantry scratch dir from launching at repo root; wrangler (viz site) cache
/train/
/.wrangler/

# local-only artifacts (never commit): preserved eval-score dumps + scratch configs
/eval_results_kept/
/configfile.txt

# Claude Code session/memory dir
.claude/

# The corpus-reasoning CODE is now ported into src/corpus_reasoning/ and IS tracked -- one repo, one
# git history. Only the large corpora/checkpoints stay outside (on /scratch).
# This entry remains only to ignore any leftover standalone clone at the repo root; that clone is
# STALE and should be deleted (`rm -rf ./corpus-reasoning`).
/corpus-reasoning/

# repo-root symlink to src/scripts/train/memexpress (the training-code hub) for quick inspection
/training
