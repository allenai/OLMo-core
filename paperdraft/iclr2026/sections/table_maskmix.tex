% TABLE-MASKMIX -- generated by paperdraft/figures/make_table_maskmix.py
% Do not hand-edit: re-run the script instead.
% Requires \usepackage{wrapfig} (and booktabs). A wraptable must NOT start in a
% paragraph's first line or inside a list -- put it just before the paragraph it
% should sit beside, or wrapfig silently drops the wrap and it overlaps the text.
% CAVEATS -- the caption is gone, so these belong in the body text:
%   * Gain = (chunked-mix) - (pure-chunked). Both arms use the identical document-chunked
%     mask at eval; they differ only in training, where +Mix anneals an unrestricted-
%     attention coin p_standard 0.8 -> 0. Qwen3.5-4B, identical shards/ladders/hyperparams.
%   * eval_size = 500 per rung, so a gain under ~0.04 is within noise (+0.011 on FiQA is not
%     distinguishable from zero).
%   * Each number is the mean per-rung gain over the 2k-32k ladder: 5 rungs, except
%     reordering, whose ladder stops at 16k (4 rungs).
%   * Reordering and QDmatch earn their whole gain at the shallow rungs (+0.473 and -0.073
%     at 2k, +0.000 and +0.006 at the deepest). By the deepest rung the FULL-attention arm
%     has itself fallen below 0.15, so no arm has headroom left to differ.
%   * Pure-chunked training CE never descended on contradiction (1.175 -> 1.071, flat across
%     the whole run) or reordering, while FiQA and QDmatch on the identical recipe converged
%     normally. So the collapse is task-specific rather than broken infrastructure -- but it
%     is a failure to FIT the training set, and is not by itself a statement about what the
%     chunked mask can represent.
\begin{wraptable}{r}{0.46\textwidth}
\centering
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{llr}
\toprule
Task & CTC & Mask-mixing gain \\
\midrule
BEIR FiQA & $O_T(N)$ & \textbf{+0.011} \\
Outlier (scale-$k$) & $O_T(NM)$ & \textbf{+0.505} \\
QDmatch (FiQA) & $O_T(N^2)$ & \textbf{+0.031} \\
Reordering & $O_T(N^2)$ & \textbf{+0.182} \\
Contradiction & $O_T(N^2)$ & \textbf{+0.791} \\
\bottomrule
\end{tabular}
\end{wraptable}
